# Learn to have fun with data

skapa en enviroment
```bash
py -m venv venv
```

aktivera enviroment
```bash
./venv/Scripts/activate
```

installera beroenden
```bash
pip install -r ./requirements.txt
```
